---
import { Image } from "astro:assets";
import Image1 from "../../assets/img1.jpg";
import { Icon } from "astro-icon/components";
---

<section class='w-full h-full px-4'>
  <div class='container'>
    <h4 class='mb-8 flex items-center gap-x-2'>
      <span class='inline-block bg-accent size-4 rounded-full'></span>About us
    </h4>
    <span class='inline-block bg-gray w-full h-[1px] mb-16 lg:mb-32'></span>

    <div class='flex flex-col lg:flex-row gap-x-8 items-center justify-between'>
      <div class='flex-1'>
        <h2 class='mb-16'>One cannot possibly accept the</h2>

        <p class='leading-8 mb-8'>
          By all means, the basic layout for segments of the primary element
          must take into account the possibility of The Enhancement of
          Reasonable Access In short, the functional testing makes no difference
          to this linguistic approach.
        </p>

        <div class='grid grid-cols-2 gap-y-4'>
          <div class='flex items-center gap-x-4'>
            <div
              class='flex items-center justify-center size-9 bg-accent rounded-full'
            >
              <Icon name='check' size={40} />
            </div>

            <p>Check item</p>
          </div>
          <div class='flex items-center gap-x-4'>
            <div
              class='flex items-center justify-center size-9 bg-accent rounded-full'
            >
              <Icon name='check' size={40} />
            </div>

            <p>Check item</p>
          </div>
          <div class='flex items-center gap-x-4'>
            <div
              class='flex items-center justify-center size-9 bg-accent rounded-full'
            >
              <Icon name='check' size={40} />
            </div>

            <p>Check item</p>
          </div>
          <div class='flex items-center gap-x-4'>
            <div
              class='flex items-center justify-center size-9 bg-accent rounded-full'
            >
              <Icon name='check' size={40} />
            </div>

            <p>Check item</p>
          </div>
        </div>
      </div>

      <div class='flex-1 mt-16 lg:mt-0'>
        <Image class='rounded-2xl' src={Image1} alt='house with garden' />
      </div>
    </div>
  </div>
</section>
